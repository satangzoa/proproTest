<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="com.Exhibition.home.dao.IDao">
	
	
	<!-- 리뷰에서 별점 보내보장  -->
	<insert id="rivewStar">
		INSERT INTO reviewStar(rnum, rating ,rid, rcontent) 
		VALUES(reviewStar_SEQ.nextval,#{param1}, #{param2}, #{param3})
	</insert>
	
	<!-- 리뷰에서 별점 가져오기  -->
	<select id="reviewList" resultType="com.Exhibition.home.dto.ReviewStar"> 
	SELECT * FROM  REVIEWSTAR ORDER BY rnum DESC
	</select>
	
	
	
	
	
	
	</mapper>